# 📘 Роль `remnawave_reality_servernames`

## 🇷🇺 Назначение

Роль обновляет список `serverNames` в REALITY‑инбаунде Remnawave Panel.  
Используется для того, чтобы добавлять домены (обычно — из SNI хостов) в конфигурацию REALITY‑inbound внутри *Config Profile*.

Это важно, потому что:
- клиент должен указывать SNI, который входит в `serverNames`;
- без этого REALITY не пропустит handshake и соединение не установится.

Роль полностью идемпотентна — повторный запуск не приведёт к дублям.

---

## ⚙️ Что делает роль

1. Загружает Config Profile по `profile_uuid`.
2. Определяет конкретный inbound:
   - по inbound_tag, если он указан;
   - иначе по inbound_uuid.
3. Извлекает блок `streamSettings → realitySettings`.
4. Берёт существующие `serverNames`.
5. Объединяет:
   - старые `serverNames`;
   - новые домены из переменной `remnawave_reality_names`.
6. Заменяет inbound в конфигурации.
7. Делает PATCH `/api/config-profiles`.

---

## 🔧 Входные переменные

| Переменная | Обязательна | Описание |
|-----------|-------------|----------|
| `remnawave_panel_api_base` | ✔️ | URL API панели, например: `https://panel.example.com/api` |
| `remnawave_panel_api_token` | ✔️ | API‑токен панели |
| `remnawave_reality_profile_uuid` | ✔️ | UUID Config Profile |
| `remnawave_reality_inbound_tag` | ❗ либо один из | Тег inbound (предпочтительный метод) |
| `remnawave_reality_inbound_uuid` | ❗ либо один из | UUID инбаунда, если нет тега |
| `remnawave_reality_names` | ✔️ | Список FQDN, которые нужно добавить |

---

## 🔄 Пример вызова роли

```yaml
- name: Patch REALITY serverNames
  include_role:
    name: remnawave_reality_servernames
  vars:
    remnawave_panel_api_base: "https://remna.example.com/api"
    remnawave_panel_api_token: "{{ api_token }}"
    remnawave_reality_profile_uuid: "a1b2c3d4-xxxx-yyyy-zzzz-1234567890"
    remnawave_reality_inbound_tag: "VLESS TCP REALITY (DS)"
    remnawave_reality_names:
      - edge-fra-01.digitalstreamers.xyz
      - edge-hel-01.digitalstreamers.xyz
```

---

## 🧠 Как это связано с хостами?

Обычно роль вызывается **из роли `remnawave_add_host`**, чтобы SNI, заданные у хоста, автоматически попадали в `serverNames`.

Например, если хост имеет:

```yaml
sni: "edge-fra-01.digitalstreamers.xyz,edge-fra-02.digitalstreamers.xyz"
```

То REALITY‑inbound автоматически получит:

```json
"serverNames": [
  "edge-fra-01.digitalstreamers.xyz",
  "edge-fra-02.digitalstreamers.xyz"
]
```

---

## 📌 Где используется

- **При создании или обновлении хоста** (runbook `remnawave_add_host`)
- **При миграции с Marzban**, когда domain/SNI подтягиваются автоматически
- **При добавлении новых доменных масок под REALITY**

---

## 🛠 Примечания

- Роль **не трогает другие инбаунды** и **не меняет порядок элементов**.
- Работает только с REALITY‑inbound.
- ServerNames всегда уникализируются.
- Поддерживает старый и новый формат API (`response.*`).
